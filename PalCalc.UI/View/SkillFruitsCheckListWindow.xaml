<adonisControls:AdonisWindow x:Class="PalCalc.UI.View.SkillFruitsCheckListWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:local="clr-namespace:PalCalc.UI.View"
                             xmlns:vm="clr-namespace:PalCalc.UI.ViewModel"
                             xmlns:itl="clr-namespace:PalCalc.UI.Localization"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                             xmlns:util="clr-namespace:Wpf.Util"
                             xmlns:behaviors="clr-namespace:PalCalc.UI.View.Behaviors"
                             mc:Ignorable="d"
                             d:DataContext="{x:Static vm:SkillFruitsCheckListViewModel.DesignerInstance}"
                             IconVisibility="Collapsed"
                             WindowStyle="ToolWindow"
                             WindowStartupLocation="CenterOwner"
                             Title="{Binding Title.Value}"
                             Height="450" Width="600"
                             behaviors:WindowPlacementBehavior.Key="SkillFruits"
                             behaviors:WindowPlacementBehavior.Mode="Size"
                             >
    <Grid>
        <Grid.Resources>
            <Style TargetType="ScrollBar" BasedOn="{StaticResource {x:Type ScrollBar}}">
                <Setter Property="adonisExtensions:ScrollBarExtension.ExpansionMode" Value="AlwaysExpand" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView Grid.Row="0"
                  ItemsSource="{Binding AllEntries}"
                  adonisExtensions:GridViewSortExtension.AutoSort="False"
                  util:GridViewSort.AutoSort="True"
                  x:Name="m_ListView"
                  PreviewKeyDown="m_ListView_KeyDown"
                  >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Padding" Value="2,3,0,3" />

                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="{itl:LocalizedText Code=LC_SKILL_FRUITS_CHECKLIST_SKILL}"
                                        DisplayMemberBinding="{Binding Skill.Name.Value}"
                                        util:GridViewSort.PropertyName="Skill.Name.Value"
                                        Width="200"
                                        />
                        <GridViewColumn Header="{itl:LocalizedText Code=LC_SKILL_FRUITS_CHECKLIST_FRUIT}"
                                        DisplayMemberBinding="{Binding FruitName}"
                                        util:GridViewSort.PropertyName="FruitName"
                                        Width="250"
                                        />
                        <GridViewColumn util:GridViewSort.PropertyName="IsEnabled">
                            <GridViewColumn.Header>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding AllItemsEnabled}" Margin="4,1,5,0" />
                                    <TextBlock Text="{itl:LocalizedText LC_COMMON_ENABLED}" />
                                </StackPanel>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsEnabled}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

        <UniformGrid Grid.Row="1" Columns="2" HorizontalAlignment="Right" Margin="5">
            <Button
                Margin="2,0"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="{itl:LocalizedText LC_COMMON_SAVE}"
                />

            <Button
                Margin="2,0"
                MinWidth="60"
                Command="{Binding CancelCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="{itl:LocalizedText LC_COMMON_CANCEL}"
                />
        </UniformGrid>
    </Grid>
</adonisControls:AdonisWindow>
